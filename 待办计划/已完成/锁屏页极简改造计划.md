# 锁定页简约改造

## TL;DR

锁屏页改为 Apple 风格简约设计：头像 + 名字 + 大锁图标 + "已锁定"文字。点击锁图标展开密码输入框，输入密码解锁。不再直接显示密码表单。

## 待办

### T1: LockPage.tsx 重构
- 新增 `showInput` state（默认 false）
- 渲染顺序：Avatar(80px) → 名字(Title) → 锁图标(LockOutlined, 64px, 可点击) → "已锁定"(Text secondary)
- 锁图标点击 → `setShowInput(true)`，密码输入区用动画展开（max-height transition 或 opacity+fadeIn）
- 密码输入区：Input.Password(autoFocus, size="large") + "解锁"按钮，原有 handleUnlock 逻辑不变（溶解动画 + unlock + window.location.href）
- 错误提示：密码错误时 message.error，不隐藏输入区（保持展开态让用户继续尝试）
- 清理 unused imports（如有）

### T2: LockPage.module.css 同步
- 新增 `.lockIcon`：font-size 64px, cursor pointer, color var(--text-secondary), hover 变 var(--color-info), transition 200ms
- `.inputArea`：overflow hidden + max-height 0→200px transition（输入区展开动画）
- `.inputArea.expanded`：max-height 200px
- 保留原有 avatar/title/subtitle 样式 + enterElement stagger 动画（仅头像/名称/锁/文字四元素，延迟 0/60/120/180ms）
- 保留 dissolving 样式 + lockPageFadeIn
- 删除不再使用的 `.form`/`.submitItem`

### T3: 验证
- `npx tsc -b` 零错误 + `npm run build` 成功
- `docker cp dist` 热部署 + 浏览器验证

## Must NOT
- 不改后端、不引入新依赖、不新加 Emoji
- 保留 dissolve 动画 + sessionStorage 解锁回跳路径
- 保留 idleTimer 锁定触发路径（LockContext.lock()）
