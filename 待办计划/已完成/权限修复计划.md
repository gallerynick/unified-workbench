# 越权修复 + 模板可见性移除

## TL;DR (For humans)

用户已确认的权限规则（m3310-m3334）：
1. **admin 对所有 private 数据无权查看/管理/编辑**（用户原话）
2. **project 模块**：admin 无特殊权限，与成员一视同仁，仅可见列表中可见
3. **template**：属于系统设置，管理=仅管理员、使用=全员公开 → **彻底移除可见性概念**（用户判定可见性是后续私自加的）
4. **content.update**：admin 不可管理私有内容 → 对齐 delete 的 own+designated 模式
5. form/vote 属于公众内容（合理不修改）；link_relation 是通用跨模块表（不属 project，已单独记录）

## 修复范围（3 组，可并行）

### 修复 A：template 移除可见性 + 管理仅管理员

**后端（4 文件）**
- `models/template.py`：删除 `visibility` 列（L40-42）、`restricted_users` 列（L43）、`from app.core.visibility import Visibility`（L14）
- `schemas/template.py`：确认无 visibility 残留（当前 TemplateCreate/Update/Response 均无，无需改）
- `services/template.py`：
  - 删除 `_visibility_get_check`（L20-28）、`_admin_can_manage_own_designated`（L31-40）、`from app.core.visibility import Visibility`（L11）、`from app.services.visibility import check_visibility`（L14）
  - `list_templates`：移除 user_id 参数及可见性分支（L78-93），永远全量 select(Template)
  - `get_template`：移除 user_id 参数及 L152-155 可见性检查
  - `update_template`/`delete_template`：权限改为 **仅 admin**（`if current_user.role != UserRole.ADMIN: 403 "仅管理员可操作"`），删除 own+designated 逻辑
- `api/templates.py`：
  - 管理端点（POST /、PUT /{id}、DELETE /{id}、POST /import）加 `Depends(require_admin)`（从 `app.core.deps` 导入）
  - list/get/export 保持 `get_current_user`（全员登录可见）
  - list 调用 `list_templates(db, page, page_size, category, search, location)`（去掉 user_id）；get 调用 `get_template(db, template_id)`

**前端（3 文件）**
- `types/template.ts`：Template/TemplateCreate/TemplateUpdate 删除 `visibility`、`restricted_users` 字段 + `import type { Visibility }`（L1、L23-24、L34-35、L43-44）
- `api/templates.ts`：无需改（body 透传，类型已清）
- `pages/templates/TemplateManagement.tsx`：
  - 删除 import `VisibilitySetting`（L34）、`Visibility`（L35）
  - 删除 state `visibility`（L78）、`restrictedUsers`（L79）
  - `openCreateModal`/`openEditModal`（L122-143）删除 visibility/restrictedUsers 设置
  - `handleModalOk`（L184-185/L200-201）删除 visibility 传递
  - 弹窗 L363-372 删除可见性 Form.Item + VisibilitySetting 块
  - 权限说明 Modal（L438-458）：删"可见范围"（公开/私有/指定用户）列表，改为说明"模板由管理员统一维护，工作台全员可见可用"

**迁移**：新建 `alembic/versions/035_drop_template_visibility.py`（down_revision="034"）：
- `op.drop_column("template", "restricted_users")` + `op.drop_column("template", "visibility")`（先 restricted 再 visibility，SQL 幂等写法尽量用 IF EXISTS；注意 alembic 链 033 的 down_revision 错误为既有缺陷，部署时如链断裂用手动 SQL + alembic_version 置 035）

### 修复 B：project 模块 admin 一视同仁

**`services/project_common.py`（3 处）**
1. `is_project_member`（L26-33）：**删除 L30-31 `if user.role == UserRole.ADMIN: return True`**，只保留 owner 或 member_ids 命中
2. `require_project_member`（L36-45）：在 is_project_member 之前插入可见性检查——private 且非 owner → 404 "项目不存在"（复用主模块规则）；restricted 走 check_visibility（从 app.core.permissions 导入）；public 放行。然后 is_project_member 不通过 → 403 "无权访问该项目"
3. `require_project_section_permission`（L48-70）：**删除 L60 中 `or user.role == UserRole.ADMIN`**，只保留 owner 放行

**`services/project.py`（主模块，2 处）**
- `update_project`（L115-120）：删除 `and current_user.role != UserRole.ADMIN` 条件 → 非 owner 一律走成员校验（is_project_member + info 分区），admin 不再自动放行
- `delete_project`（L148-153）：同上

**子模块 service（6 个）**：member/proposal/todo/meeting/change/event 均通过 project_common 的两个函数，无需逐个改——但需 grep 确认没有绕过 project_common 的独立 admin 判断。

### 修复 C：content.update 对齐 delete

**`services/content.py` `update_content`（L85-89）**：将权限判断改为与 delete_content（L124-136）一致的 own+designated 模式：
```python
if content.owner_id != current_user.id:
    if current_user.role != UserRole.ADMIN:
        raise HTTPException(status_code=403, detail="无权修改此内容")
    if content.visibility != Visibility.PUBLIC and (
        not content.restricted_users
        or str(current_user.id) not in content.restricted_users
    ):
        raise HTTPException(status_code=403, detail="无权修改此内容")
```
即：owner 直接可改；admin 仅可修改 PUBLIC 或 restricted_users 含 admin 的内容（private 被挡）。

## 执行顺序

1. Wave 1（3 路并行）：
   - Agent A：修复 A 后端（models/schemas/services/api + 迁移 035）+ py_compile
   - Agent B：修复 B（project_common + project.py）+ grep 子模块确认无绕过
   - Agent C：修复 C（content.py）+ py_compile
2. Wave 2：修复 A 前端（types + TemplateManagement.tsx）+ tsc -b
3. Wave 3：F 验证——py_compile 全部 + tsc -b + npm run build + 部署（docker cp 或 start.sh）+ curl 冒烟：
   - template：admin 可建/改/删；普通用户 list/get/export 200；普通用户 PUT/DELETE 403
   - project：admin 对他人 private 项目子模块 404/403；admin 对自己项目正常；非 admin 成员正常
   - content：admin 改他人 private 内容 403；admin 改 PUBLIC 内容 200；owner 正常

## Must NOT
- 不改 form/vote（公众内容，用户确认合理）
- 不改 link_relation（通用跨模块表，非 project 专属）
- 不引入新依赖、无 Emoji
- 前端除修复 A 指定文件外不触碰其他页面

## 验收标准
- 所有 private 数据：admin 不能查看/管理/编辑（403/404）
- template 管理仅 admin、使用全员公开、无任何可见性 UI/字段残留
- project 子模块：admin 与成员一视同仁
- content.update 与 delete 权限一致
