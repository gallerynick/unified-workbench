# link_relation 说明文件 + 仓库目录同步更新

## TL;DR (For humans)

1. 在仓库根目录创建 `link_relation说明.md`：记录 link_relation 通用双向关联表的设计定位、当前状态、与 project 模块的关系澄清。
2. 目录同步：仓库新增了 `历史文件/` 目录（含 `设计审计对比报告.md`），需把该目录（以及现状已存在但基准文件缺失的 `待办计划/`、`问题记录/`、`项目架构/`）补入 AGENTS.md、README.md、项目基定/文件目录及说明.txt 的目录结构。

## 背景（已探索确认）

- 仓库根目录现状：AGENTS.md / DESIGN.md / README.md / .unified-workbench / 待办计划 / 历史文件(含设计审计对比报告.md) / 问题记录 / 项目基定 / 项目架构 / 项目开发日志 / 项目文件 / .gitignore 等
- AGENTS.md「仓库结构」当前仅列：项目基定/ 项目开发日志/ 项目文件/ .unified-workbench/ AGENTS.md（缺 待办计划/ 历史文件/ 问题记录/ 项目架构/）
- README.md「项目结构」仅展示 backend/frontend/nginx/mediamtx 等代码结构（缺工作区目录）
- 项目基定/文件目录及说明.txt 有「一至六」节（基定/文件/日志/待办计划/问题记录/项目架构），缺「历史文件」
- link_relation 事实（已由 explore 核实）：
  - 模型 `backend/app/models/link_relation.py`（33 行）：表 link_relation，字段 id/source_type/source_id/target_type/target_id/created_at，**无任何 FK**，type 为自由字符串
  - API `backend/app/api/link_relations.py`（prefix `/link-relations`）：POST 创建(双向双写)/GET list/GET linked-entities/DELETE 四参删除
  - 诞生于迁移 034（project_module_redo）但**不属于 project 模块**：project 内 Tab 间关联用外键（如 TodoTask.proposal_id）
  - 前端 `api/link-relations.ts` 客户端已写好，**当前无任何页面调用**（仅 types/link-relation.ts 被引用）
  - 定位：通用跨模块双向关联表（如未来 content↔note 关联），当前未接入 UI

## 待办

### T1: 创建 link_relation说明.md（仓库根目录）
- 路径：`/Users/gallerynick/Documents/Project/Project_UnifiedWorkbench/link_relation说明.md`
- 内容结构：
  1. 定位：通用双向关联表（非 project 专属）
  2. 数据模型：表结构/字段/无 FK 设计原因（type 自由字符串 + UUID 双写）
  3. 行为：create/delete 双向双写、linked-entities 去重查询
  4. 与 project 的关系澄清：诞生于 034 迁移的历史原因；project 内 Tab 间关联实际用外键（proposal_id），未使用此表
  5. 当前状态：前端客户端已封装、无页面使用、未接入任何模块
  6. 未来用途建议：跨模块实体关联（content↔note 等）
- 简体中文、无 Emoji、与 项目架构/ 文档风格一致

### T2: AGENTS.md 仓库结构补全
- `AGENTS.md` 的「## 仓库结构」代码块中补充：
  ```
  ├── 待办计划/                  # 【读写】计划规划与记录
  ├── 历史文件/                  # 【只读】历史归档文档（设计审计对比报告等）
  ├── 问题记录/                  # 【读写】疑难问题与解决方案记录
  ├── 项目架构/                  # 【只读】架构设计文档（详见文末索引）
  ```
  位置：项目开发日志/ 之后、项目文件/ 之前（按字母序或逻辑序，保持现有风格）
- 不改动其他章节

### T3: README.md 项目结构补充
- `README.md` 的「## 项目结构」代码块（L94-118）说明的是代码结构，在代码结构上方或下方补充工作区目录说明：
  ```
  工作区目录：
  ├── 项目基定/         # 项目规格（只读）
  ├── 项目文件/         # 前后端代码
  ├── 项目开发日志/     # 开发日志
  ├── 待办计划/         # 计划文件
  ├── 历史文件/         # 历史归档文档
  ├── 问题记录/         # 问题与解决记录
  ├── 项目架构/         # 架构设计文档
  └── link_relation说明.md  # 通用关联表设计说明
  ```

### T4: 项目基定/文件目录及说明.txt 补「历史文件」
- 在「六、项目架构」后追加：
  ```
  七、历史文件
  1.路径：Project_UnifiedWorkbench/历史文件
  2.说明：存放历史归档文档（如设计审计对比报告等），保留设计决策与审计记录
  3.权限：只读
  ```

### T5: 验证
- 确认 4 个文件修改完成、link_relation说明.md 内容准确（与代码事实一致：无 FK、双向双写、无页面使用、project 用外键）
- git 提交

## Must NOT
- 不改动代码文件、不改 AGENTS.md 除「仓库结构」外其他章节
- 不删除或改写 历史文件/ 现有内容
- 无 Emoji
