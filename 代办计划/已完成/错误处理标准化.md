# 错误处理标准化计划

## 背景

扫描 100 个 TSX/TS 文件，34 个文件存在错误处理问题。

---

## 一、标准错误处理模式（目标）

```ts
try {
  setLoading(true);
  const res = await fetchData();
  if (res.code === 0) {
    // 成功
  } else {
    message.error(res.msg || '操作失败');
  }
} catch (err: unknown) {
  const msg = err instanceof Error ? err.message : '操作失败';
  message.error(msg);
} finally {
  setLoading(false);
}
```

---

## 二、问题清单

### 🔴 致命（15 处）
| 问题 | 文件 | 行号 |
|------|------|------|
| 无 try/catch 的 await | FinanceManagement.tsx | 110, 160 |
| 空 catch {} | UserContext.tsx | 32 |
| 空 catch {} | ThemeContext.tsx | 29, 55, 66 |
| 空 catch {} | VotePopup.tsx | 33 |
| 空 catch {} | SystemSettings.tsx | 38, 45 |
| 注释 ignore 的空 catch | FormResponses.tsx | 46 |
| .catch(()=>{}) | StreamWatch.tsx | 38 |
| .catch(()=>{}) | StreamStudio.tsx | 590 |
| .catch(()=>{}) | WhepClient.ts | 241 |
| 无 loading 管理 | FinanceManagement.tsx | 47-62 |

### 🟠 高（~24 处）Loading 悬挂
catch 中无 `finally { setLoading(false) }`，异常时 loading 永为 true：
AnnouncementManagement(3处)、ContactManagement(3处)、ContentManagement、NoteManagement、SidebarManagement(2处)、TagManagement(2处)、SiteSettings、UserPersonalization、TopologyManagement(2处)、SecretManagement、CalendarPage(2处)、AnnouncementsWidget、NotificationsWidget(2处)、TodosWidget、CalendarWidget

### 🟡 中
- ~10 文件使用通用 `message.error('操作失败')` 无具体错误
- 6 文件混用两种 catch 模式（简单 catch vs 详细 err:unknown）
- StreamStudio 中声明 err 变量但未使用

---

## 三、执行计划

| 优先级 | 任务 | 涉及文件 |
|--------|------|---------|
| P0 | FinanceManagement delete 添加 try/catch | 1 文件 |
| P0 | 空 catch 添加至少 console.warn | 6 文件 |
| P1 | 15 文件添加 finally setLoading(false) | 15 文件 |
| P2 | 统一 catch 模式为 `err: unknown` | ~10 文件 |
| P3 | 错误信息具体化 | ~10 文件 |
